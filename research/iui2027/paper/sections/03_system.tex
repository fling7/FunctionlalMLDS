\section{System}

Figure~\ref{fig:architecture} separates the authoring loop from the runtime path.
Both use the same identifiers and relationships, but AI is used only to propose
semantic content; deterministic checks decide whether an artifact or action is
admissible.

\input{figures/architecture}

\subsection{Interaction model}

\systemname{} represents an interaction at several linked levels. A
\emph{scenario step} captures a user-visible action, such as asking an exhibit
agent to describe a dinosaur skeleton. A \emph{capability use} identifies the
operation, the responsible provider, and the spatial targets. The capability is
connected through a \emph{runtime binding} to a concrete backend action. An
\emph{assertion} describes the outcome that should be observable, and a
\emph{validation case} links a stimulus to that expected outcome.

The required execution path is:

\begin{quote}
\texttt{ScenarioStep $\rightarrow$ CapabilityUse $\rightarrow$ Capability
$\rightarrow$ RuntimeBinding $\rightarrow$ RuntimeAction}.
\end{quote}

This path intentionally prevents a scenario step from invoking an endpoint
without an explicit agent capability. Agents additionally reference their
responsible zones and grounded assets. These relationships allow validators to
reject a capability use whose provider is not responsible for its spatial
target.

\begin{table}[t]
  \caption{Running example: representing ``What is this?'' while a visitor
  selects the classroom dinosaur skeleton.}
  \Description{A table maps each interaction-model element to its dinosaur
  example instance and purpose. The chain proceeds from the selected skeleton
  through an exhibit-interpreter agent and description capability to the chat
  endpoint and a grounded-response assertion.}
  \label{tab:dinosaur-chain}
  \begin{tabular}{p{.19\linewidth}p{.29\linewidth}p{.43\linewidth}}
    \toprule
    Element & Example instance & Meaning \\
    \midrule
    Target &
    Dinosaur skeleton asset; its object group and exhibit zone &
    Stable referent selected in Unity \\
    Scenario step &
    Describe selected dinosaur skeleton &
    User-visible interaction to execute \\
    Capability use &
    Describe room object; provider: Exhibit Interpreter &
    One occurrence with an explicit provider and targets \\
    Agent &
    Exhibit Interpreter &
    Domain agent responsible for the asset \\
    Runtime binding &
    Validated \texttt{POST /chat} request and response &
    Concrete operation permitted for the capability \\
    Assertion &
    Target, provider, response entity, and route agree &
    Observable pass/fail evidence \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Generation pipeline}

The pipeline starts from a structured room description. It normalizes scene
entities, derives scene semantics, proposes agent roles and capabilities,
materializes knowledge resources, and computes agent placement. A native
\systemname{} instance then connects these products to interaction scenarios and
runtime bindings. Deterministic validators check schemas and cross-reference
invariants before artifacts are made available to Unity or the backend.

The implementation separates probabilistic and deterministic stages. Language
models can propose semantic labels, roles, knowledge, and evaluation utterances.
Identifiers, geometric placement, relationship validation, artifact
materialization, and metric computation are deterministic. Each stage records
its inputs, configuration, outputs, and hashes so that a result can be
invalidated when a relevant upstream artifact changes.

\subsection{Inspectable placement authoring}

The Unity editor exposes the generated placement as a revisioned working copy.
An author can inspect stable agent identifiers, preview a structured change and
its before/after values, apply it through deterministic regeneration and
validation, and then accept or discard the result. The last accepted placement
can be undone without restarting the backend. Content hashes provide an
optimistic-concurrency guard, and files are restored byte for byte when
regeneration fails or an applied preview is discarded. Runtime project commit
is disabled while a change awaits a decision. Free-text chat is not interpreted
as a model edit; the interface says explicitly that such text was not applied.

\subsection{Unity client and WebXR path}

The Unity client imports room assets and agent configurations, places embodied
agents in their assigned zones, and supports first-person and proximity-based
interaction. A scene-object component binds a collider to a stable model entity
rather than relying on its GameObject name. In the evaluated desktop path, a
camera ray selects that component, the object is highlighted, and the resolved
target remains stable while a text or voice request is composed. The client
sends the pinned model hash, entity and source identifiers, ray-hit metadata,
input modality, active agent, and utterance to the backend. No-target and
ambiguous states are explicit and do not silently fall through to an LLM call.

The project includes WebXR and XR Interaction Toolkit dependencies. Controller
rays can use the same selection interface, but immersive interaction is
described as a deployment path rather than an evaluated condition unless a
physical-headset test is reported.

\subsection{Agent backend and handoffs}

The backend exposes setup, chat, speech, and project-management endpoints. Each
agent has a persona, knowledge scope, capabilities, and permitted handoff
targets. A request can be answered by the active agent, delegated to a more
appropriate agent, or rejected when no grounded target or responsible
capability exists. The backend validates the model hash and entity before
mutating session state. It derives the entity's group and zone from the pinned
model rather than trusting client-supplied responsibility fields. Routing gives
an explicit asset binding precedence over group and zone responsibility; any
handoff is limited to the source agent's modeled targets. Runtime actions carry
the interaction-model identifiers needed to distinguish a successful HTTP
request from a semantically correct interaction.

\subsection{Runtime validation}

Events are written against the originating scenario, capability use, binding,
agent, and spatial target. Validation separates four questions:

\begin{enumerate}
  \item Was a syntactically valid runtime operation invoked?
  \item Was the intended spatial entity resolved?
  \item Was the responsible agent and capability selected?
  \item Did the observable response preserve the expected entity and permitted
  route?
\end{enumerate}

This separation avoids treating network success or a single token overlap as
evidence of grounded interaction. It also provides a failure location that can
be surfaced in authoring tools or logs.

\subsection{Use of machine intelligence}

The system uses language models for semantic scene interpretation, agent-role
proposal, knowledge synthesis, and conversational responses. Prompts, model
identifiers, sampling parameters, and repair attempts are versioned. No
language-model output is accepted as structurally valid without schema and
invariant checks. The reported benchmark neither invokes a model nor uses one
as a semantic judge. The Stanford Agentic Reviewer is not part of the system or
the scientific evaluation.
